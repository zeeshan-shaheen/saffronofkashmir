# CLAUDE.md — saffronofkashmir.com

Read this fully before touching anything. These rules protect a **live business website**.

---

## What this project is

- A **static website** on GitHub Pages, behind Cloudflare. Domain: https://saffronofkashmir.com
- **There is no server and no database.** You cannot run server-side code, store user data, or keep secrets in any file that ships in this repo.
- All content lives in ONE file: `data/site-data.json` — the **single source of truth**.
- `assets/admin/templates.js` turns that JSON into the site's HTML (pure functions: data → HTML strings).
- `build.js` regenerates all output files from the JSON + templates. Run with `node build.js`.
- `admin.html` + `assets/admin/admin.js` are a **browser-only** editor: it edits the JSON and commits the regenerated pages to GitHub via the GitHub API.
- If an older handoff document and the live `data/site-data.json` disagree on any value, **the JSON wins.**

---

## Repository layout

```
saffronofkashmir/
├── data/
│   └── site-data.json          ← single source of truth (ALL content lives here)
├── assets/
│   ├── admin/
│   │   ├── templates.js        ← pure render functions (UMD, runs in Node + browser)
│   │   └── admin.js            ← browser-only admin panel logic
│   ├── js/
│   │   └── main.js             ← runtime JS for the live site (no libraries, ~4 KB)
│   └── css/
│       └── style.css           ← single stylesheet, no frameworks
├── admin.html                  ← browser-only admin panel (noindex)
├── build.js                    ← Node build script
├── robots.txt                  ← Disallows /admin.html, /assets/admin/, /data/
├── llms.txt                    ← generated by build (llms.txt spec)
├── sitemap.xml                 ← generated by build
├── index.html                  ← generated by build — DO NOT hand-edit
├── products.html               ← generated by build — DO NOT hand-edit
├── recipes.html                ← generated by build — DO NOT hand-edit
├── blogs.html                  ← generated by build — DO NOT hand-edit
├── 404.html                    ← generated by build — DO NOT hand-edit
└── privacy-policy.html         ← generated by build — DO NOT hand-edit
```

---

## How a change must flow (follow this every time)

- **Content change** → edit `data/site-data.json` → `node build.js` → review diff.
- **Markup / page-output change** → edit `assets/admin/templates.js` → `node build.js` → review diff.
- **New editable field in the admin** → you MUST edit BOTH files:
  - `templates.js` so the field actually appears on the live site, AND
  - `admin.js` so it can be edited.
  - A field added only to `admin.js` saves into the JSON and is **invisible on the site**. This is the single most common mistake on this project. Do not make it.
- **NEVER hand-edit the generated HTML files** (`index.html`, `products.html`, `recipes.html`, `blogs.html`, `404.html`, `sitemap.xml`, `llms.txt`, `privacy-policy.html`). They are build output and will be overwritten.

---

## Hard rules — do not break

1. `templates.js` must stay **dual-environment (UMD)**: pure functions only. No `document`, `window`, `fetch`, DOM, or browser-only APIs inside it. It runs under Node (via `build.js`) AND in the browser (the admin).
2. Every generated page must keep: exactly **one `<h1>`**, valid/parseable **JSON-LD**, correct `canonical` + `og:` tags. `admin.html` keeps its `noindex`. `robots.txt` keeps `Disallow` for `/admin.html`, `/assets/admin/`, `/data/`.
3. Publishing must stay **idempotent and skip-unchanged**. Do not alter that behaviour.
4. **NEVER commit secrets.** No GitHub token, no API keys, no service-account files. The owner's token lives only in their browser.
5. **Never put real images into any package/zip.** Images live in the repo.
6. Apostrophes/quotes: data uses real `'` and `'`; testimonials use curly quotes. Keep them.

---

## Verification — run after EVERY change, before committing

```powershell
# 1. Syntax check (PowerShell)
node --check assets/admin/templates.js
node --check assets/admin/admin.js
node --check build.js

# 2. Build
node build.js

# 3. Parity check — save as _parity_check.py and run with python _parity_check.py
# 4. JSON-LD check — save as _check_jsonld.py and run with python _check_jsonld.py
```

Parity script (`_parity_check.py` — already exists in repo root):
```python
import re, html, os
def textof(p):
    s = open(p, encoding="utf-8").read()
    s = re.sub(r"<script.*?</script>", " ", s, flags=re.S)
    s = re.sub(r"<[^>]+>", " ", s)
    s = html.unescape(s)
    return re.sub(r"\s+", " ", s).strip()
pages = ["index.html","products.html","recipes.html","blogs.html","404.html"]
for p in pages:
    baseline = os.path.join("_parity_baseline", p + ".txt")
    if not os.path.exists(baseline): print("NO BASELINE: " + p); continue
    old = open(baseline, encoding="utf-8").read().strip()
    new = textof(p)
    if old == new: print("OK  " + p)
    else:
        ow = old.split(); nw = new.split()
        for i in range(min(len(ow),len(nw))):
            if ow[i] != nw[i]:
                print("DIFF " + p + " at word " + str(i))
                print("  old: " + " ".join(ow[max(0,i-3):i+8]))
                print("  new: " + " ".join(nw[max(0,i-3):i+8]))
                break
```

**Important:** Intentional new visible content (e.g. a badge, a new nav element) WILL show in the parity diff — that is expected. Read the diff and confirm only your intended text changed.

JSON-LD check script (`_check_jsonld.py` — already exists in repo root):
```python
import re, json
for p in ["privacy-policy.html", "index.html", "products.html"]:
    s = open(p, encoding="utf-8").read()
    blocks = re.findall(r'<script[^>]+type="application/ld\+json"[^>]*>(.*?)</script>', s, re.S)
    if not blocks: print(p + ": NO JSON-LD"); continue
    for i, b in enumerate(blocks):
        try: json.loads(b.strip()); print(p + " block " + str(i) + ": OK")
        except Exception as e: print(p + " block " + str(i) + ": INVALID - " + str(e))
```

---

## Git workflow — mandatory

- Work on a branch named `feature/<short-name>`. **Never commit to `main` directly. Never push to `main` without explicit human approval.**
- One feature = one clean commit (or a small, clear set). Write plain, descriptive commit messages.
- After each phase: show the human the **diff** and the **verification output**, then **STOP and wait** for approval before the next phase.
- Exception: simple content-flag toggles (e.g. `overlay.enabled`) mirror what the admin panel does and may go directly to `main` with explicit human instruction.
- If anything is ambiguous, **ask**. Do not improvise structural decisions on a live site.

---

## data/site-data.json — schema reference

Top-level keys and their purpose:

### `meta`
```json
{ "schemaVersion": 1, "lastPublished": "<ISO timestamp>" }
```

### `brand`
Core site identity. Key fields:
```json
{
  "name": "Saffron of Kashmir",
  "tagline": "Pampore · Est. 2004",
  "siteUrl": "https://saffronofkashmir.com",
  "logo": "logo.webp",
  "favicon": "favicon.webp",
  "ogImage": "Cover.webp",
  "phoneDisplay": "+91 7006 603060",
  "phoneTel": "+917006603060",
  "whatsappNumber": "971522613060",
  "defaultWaText": "Hi! I'd like to order from Saffron of Kashmir.",
  "email": "info@saffronofkashmir.com",
  "instagramUser": "saffron_of_kashmir",
  "foundingYear": "2004",
  "orgDescription": "...",
  "gaId": "G-9569ES4LBP",
  "fbPixelId": "1140202034977517",
  "baseCurrency": "AED",
  "currencies": {
    "AED": { "name": "UAE Dirham",   "symbol": "AED", "rate": 1,      "decimals": 2, "markup": 0 },
    "USD": { "name": "US Dollar",    "symbol": "USD", "rate": 0.2723, "decimals": 2, "markup": 0 },
    "INR": { "name": "Indian Rupee", "symbol": "₹",   "rate": 22.73,  "decimals": 0, "markup": 0 },
    "SAR": { "name": "Saudi Riyal",  "symbol": "SAR", "rate": 1.021,  "decimals": 2, "markup": 0 },
    "QAR": { "name": "Qatari Riyal", "symbol": "QAR", "rate": 0.991,  "decimals": 2, "markup": 0 },
    "OMR": { "name": "Omani Rial",   "symbol": "OMR", "rate": 0.1048, "decimals": 3, "markup": 0 }
  }
}
```
- `rate`: exchange rate from AED (base) to this currency.
- `markup`: percentage applied on top of the rate. `price = aed * rate * (1 + markup/100)`. Positive = mark up (e.g. shipping premium). Negative = mark down (e.g. local market pricing). 0 = pure conversion.
- `decimals`: displayed decimal places (OMR uses 3, INR uses 0).
- Prices on the site are stored in AED. All other currencies are converted client-side in `main.js`.

### `seo`
Per-page SEO fields. Keys: `home`, `products`, `recipes`, `blog`. Each has: `title`, `description`, `ogTitle`, `ogDescription`.

### `products` (array, 6 items)
Each product:
```json
{
  "id": "saffron-1g",
  "name": "Mongra Saffron — 1g",
  "category": "saffron",
  "featured": true,
  "status": "available",       // "available" | "out_of_stock" | "coming_soon"
  "sale": null,                // null or { "price": 55, "label": "Save 15%", "until": "2026-12-31" }
  "waText": "Hi! I'd like to order the 1g Mongra Saffron tin.",
  "price": 65,                 // base price in AED
  "unitLabel": "/ tin",
  "image": "tin-1.webp",
  "imageAlt": "...",
  "description": "...",
  "details": [...]
}
```
- `status` controls the badge displayed on cards (Out of Stock / Coming Soon).
- `sale.price` is the discounted AED price; `sale.label` is optional badge text; `sale.until` is ISO date.
- All prices are in AED — the currency switcher converts them client-side.

### `recipes` (array, 6 items)
Each recipe: `id`, `title`, `category`, `description`, `image`, `imageAlt`, `time`, `servings`, `ingredients` (array), `steps` (array), `tips` (string).

### `posts` (array, 5 items)
Each post: `id`, `title`, `category`, `excerpt`, `body` (mini-markdown, `## ` for subheadings, blank lines for paragraphs), `image`, `imageAlt`, `date` (YYYY-MM-DD).

### `faq` (array)
Each item: `question`, `answer` (mini-markdown supported).

### `testimonials` (array)
Each item: `name`, `location`, `text` (curly quotes), `rating` (1–5).

### `home`
Homepage-specific content: `hero` (headline, subheadline, cta, image, imageAlt), `about` (heading, body mini-markdown), `whyUs` (array of {icon, title, text}), `featuredHeading`.

### `footer`
`about` (short blurb), `locationLine`, `bottomRight`.

### `overlay`
First-visit discount popup:
```json
{
  "enabled": true,
  "heading": "Welcome — 10% Off Your First Order",
  "text": "...",
  "discountText": "10% off your first order",
  "buttonLabel": "Claim My Discount",
  "successText": "Thank you! Your discount code is on its way — check your inbox.",
  "image": "",
  "privacyHref": "privacy-policy.html",
  "formEndpoint": "https://saffronofkashmir.us5.list-manage.com/subscribe/post?u=236b4cb10d93993487bd4853a&id=aace8dda1c&f_id=00e8a9e1f0"
}
```
- `enabled: false` = no overlay HTML injected anywhere (zero site impact).
- `formEndpoint` is the Mailchimp embedded form action URL. The honeypot field name is auto-derived from the `u=` and `id=` params.
- Overlay triggers after 4 seconds OR first scroll, whichever comes first. Uses `sessionStorage` (seen this session) and `localStorage` (already subscribed — never show again).

---

## templates.js — key functions

All functions are pure (data → HTML string). No DOM, no fetch, no side effects.

| Function | Purpose |
|---|---|
| `esc(s)` | HTML-escape a value |
| `waUrl(brand, text)` | Build WhatsApp link |
| `inlineMd(brand, s)` | Mini-markdown: `[label](url)`, `**bold**`, `*em*`, `wa:` links |
| `plainMd(s)` | Strip mini-markdown to plain text (for JSON-LD) |
| `bodyToHtml(brand, body)` | Blog body: blank lines → `<p>`, `## ` → `<h3>` |
| `ld(obj)` | Wrap object in `<script type="application/ld+json">` |
| `statusBadge(status)` | Badge HTML for out_of_stock / coming_soon |
| `statusAvailability(status)` | schema.org availability URL |
| `productPriceHtml(p)` | Price HTML with `data-price="<aed>"` attrs for JS conversion |
| `renderOverlayHtml(data)` | Overlay HTML (returns `''` if `!enabled` or no endpoint) |
| `currencySelect(b)` | `<select id="sok-curr">` with options from `brand.currencies` |
| `head(data, page, opts)` | `<head>` block (SEO, GA, FB pixel, JSON-LD) |
| `header(data, current)` | `<body>` open + nav with currency select |
| `footer(data)` | Footer + back-to-top + overlay + `<script src="main.js">` |
| `breadcrumbs(label)` | Breadcrumb nav |
| `renderIndex(data)` | index.html |
| `renderProducts(data)` | products.html |
| `renderRecipes(data)` | recipes.html |
| `renderBlogs(data)` | blogs.html |
| `render404(data)` | 404.html |
| `renderPrivacyPolicy(data)` | privacy-policy.html |
| `renderSitemap(data, dateStr)` | sitemap.xml |
| `renderLlms(data)` | llms.txt |
| `renderAll(data)` | Returns `{ filename: html, ... }` for all pages — both `build.js` and the admin publish loop iterate this |

**Adding a new generated page:** add a `renderXxx(data)` function, add it to `renderAll`'s return object, add it to the public API `return { ... }` at the bottom. `build.js` picks it up automatically.

### Price rendering and currency conversion

`productPriceHtml(p)` renders prices with `data-price="<aed_value>"` on every price `<span>` or `<s>`. Example output:
```html
<div class="p-price">
  <span data-price="65">AED 65</span>
  <span class="unit">/ tin</span>
</div>
```
`main.js` finds all `[data-price]` elements and converts them to the active currency using:
```
converted = aed * rate * (1 + markup / 100)
```
formatted with `Intl.NumberFormat`.

### Currency select in nav

`currencySelect(b)` generates a `<select id="sok-curr">` from `brand.currencies`. Each `<option>` carries `data-rate`, `data-symbol`, `data-decimals`, `data-markup`. JS reads these from the DOM — no global variable needed.

---

## main.js — runtime modules

All wrapped in one IIFE (`(function () { 'use strict'; ... })()`). No libraries. Current modules (in order):

1. **Mobile nav toggle** — `.nav-toggle` ↔ `.nav-links.open`
2. **Back to top** — `.back-top` shows after 600px scroll
3. **Filter bars** — `[data-filter-bar]` / `[data-filter-target]` / `[data-category]` for products and blog
4. **WhatsApp tracking** — delegated click on `a[href*="wa.me"]` → `gtag('event', 'whatsapp_click', { item })`
5. **Currency switcher** — geo-detects via `https://ipapi.co/json/` (free, 1000 req/day) on first visit; maps country code to currency; saves to `localStorage` key `sok_currency`; responds to `<select>` change; applies `Intl.NumberFormat` to all `[data-price]` elements
6. **First-visit overlay** — 4 s delay OR first scroll trigger; `sessionStorage` key `sok_overlay_seen`; `localStorage` key `sok_overlay_done`; JSONP Mailchimp submit (endpoint `/post?` → `/post-json?` + `&c=sokMcCb`); ESC + backdrop dismiss

### Country → currency mapping (in main.js)
```
AE → AED   IN → INR   US → USD   SA → SAR   QA → QAR   OM → OMR
KW → AED   BH → AED   (all others) → USD
```

---

## admin.js — sections

The admin panel is a single-page app. `SECTIONS` maps section keys to render functions. The sidenav buttons have `data-sec="<key>"`.

| Section key | Function | Purpose |
|---|---|---|
| `dashboard` | `secDashboard()` | Read-only stats, content QA, quick links to GA/Search Console/PageSpeed |
| `home` | `secHome()` | Homepage hero, about, why-us |
| `products` | `secProducts()` | Product list with status, sale toggle, images |
| `recipes` | `secRecipes()` | Recipe list |
| `posts` | `secPosts()` | Blog post list |
| `faq` | `secFaq()` | FAQ items |
| `testimonials` | `secTestimonials()` | Testimonial items |
| `brand` | `secBrand()` | Brand identity, contact, GA/FB IDs |
| `seo` | `secSeo()` | Per-page SEO titles and descriptions |
| `media` | `secMedia()` | Image upload and browser |
| `settings` | `secSettings()` | GitHub token, repo, branch |
| `overlay` | `secOverlay()` | Discount overlay toggle, content, Mailchimp endpoint |
| `currencies` | `secCurrencies()` | Per-currency exchange rate, price markup %, decimal places |

### Field builder helpers (in admin.js)
- `f(label, path, opts)` — text/select/checkbox/textarea field; `path` is dot-notation into `S.data`
- `num(label, path, opts)` — number field (wraps `f` with `coerce: 'number'`)
- `imgField(label, path, opts)` — image field with upload button
- `card(icon, title, inner, extra)` — card wrapper
- `A` — alias for `SOKTemplates.esc`

### openPreview map
```js
{
  'index.html': SOKTemplates.renderIndex,
  'products.html': SOKTemplates.renderProducts,
  'recipes.html': SOKTemplates.renderRecipes,
  'blogs.html': SOKTemplates.renderBlogs,
  '404.html': SOKTemplates.render404,
  'privacy-policy.html': SOKTemplates.renderPrivacyPolicy
}
```

---

## Features built to date

| Feature | Files changed | Notes |
|---|---|---|
| Product status badges | `templates.js`, `admin.js`, `data/site-data.json` | `status`: available / out_of_stock / coming_soon |
| Sale / discount pricing | `templates.js`, `admin.js`, `data/site-data.json` | `sale`: { price, label, until } per product |
| WhatsApp click tracking | `main.js` | `gtag('event', 'whatsapp_click')` |
| llms.txt | `templates.js`, `build.js` | Auto-generated from brand + page descriptions |
| Admin Dashboard | `admin.js`, `admin.html` | Stats, content QA, quick links |
| First-visit discount overlay | `templates.js`, `main.js`, `style.css`, `admin.js`, `admin.html`, `data/site-data.json` | Mailchimp JSONP; currently **enabled** |
| Privacy Policy page | `templates.js`, `data/site-data.json`, `sitemap.xml` | Generated page at `/privacy-policy.html` |
| Multi-currency switcher | `templates.js`, `main.js`, `style.css`, `admin.js`, `admin.html`, `data/site-data.json` | AED base; geo-detect via ipapi.co; localStorage preference |
| Per-currency price markup | `templates.js`, `main.js`, `admin.js`, `data/site-data.json` | `markup` % per currency; admin-editable |

---

## Things you must NOT do on this project

- Do not add a server, a database, or any server-side code.
- Do not store user/customer data in this repo or in client-side code. Data capture goes to a third-party service the human names.
- Do not rebuild analytics that Google Analytics or Search Console already provide.
- Do not add rating/review structured data (`aggregateRating`, review stars) unless real, displayed customer reviews exist — faking it breaks Google policy.
- Do not create indexable Markdown copies of pages for "AI SEO" — duplicate content hurts rankings.
- Do not make pop-ups that cover content on mobile page load (Google penalises intrusive interstitials).
- Do not refactor, rename, reformat, or "tidy" files you were not asked to change.
- Do not add new currencies to the switcher without adding them to `data/site-data.json` under `brand.currencies` first — the template generates options from the JSON, not from a hardcoded list.
